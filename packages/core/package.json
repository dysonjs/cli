{
  "name": "@dysonic/dy-cli-core",
  "version": "1.0.1",
  "license": "MIT",
  "description": "Core package providing shared command contracts, execution abstractions, and helper utilities for dy-cli",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {
    "commander": "12.1.0",
    "execa": "5.1.1",
    "fs-extra": "10.1.0",
    "ts-node": "10.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "9.0.12"
  },
  "scripts": {
    "clean": "rimraf dist && rimraf coverage && rimraf node_modules",
    "prebuild": "rimraf dist"
  }
}
