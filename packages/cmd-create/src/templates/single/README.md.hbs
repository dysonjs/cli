# {{projectName}}

Generated by `dy-cli` for a single-package workflow.

## Commands

```bash
pnpm install
pnpm run lint
pnpm run prettier
pnpm run build
pnpm run test
pnpm exec dy-cli version --set 0.0.1
pnpm run release:dry-run
```

## Release

The generated project includes `.github/workflows/release.yml` for preparing release pull requests.
Set `NPM_TOKEN` in your CI or local environment before publishing.
