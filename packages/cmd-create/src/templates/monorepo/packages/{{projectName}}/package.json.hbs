{
  "name": "{{scopedPackageName}}",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "browser": "dist/index.umd.js",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build": "dy-cli build",
    "build:types": "dy-cli build --types",
    "build:umd": "dy-cli build --umd",
    "test": "dy-cli test",
    "test:coverage": "dy-cli test --coverage",
    "publish": "dy-cli publish",
    "publish:dry-run": "dy-cli publish --dry-run",
    "publish:beta": "dy-cli publish --tag beta",
    "publish:beta:dry-run": "dy-cli publish --tag beta --dry-run",
    "clean": "rimraf dist && rimraf coverage && rimraf node_modules",
    "prebuild": "rimraf dist"
  }
}
